import React from 'react'
// import useInput from '../../hooks/useInput'

const Input = (props) => {
    // const valueAndOnchange = useInput("");
    // console.log(valueAndOnchange)

    return (
        <input {...props} />
    )
}

export default Input
